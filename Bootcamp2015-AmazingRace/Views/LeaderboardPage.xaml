<Page
    x:Class="Bootcamp2015.AmazingRace.Views.LeaderboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Bootcamp2015.AmazingRace.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:Bootcamp2015.AmazingRace.ViewModels"
    xmlns:micro="using:Caliburn.Micro"
    micro:Bind.AtDesignTime="True"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel HorizontalAlignment="Left" VerticalAlignment="Top" Width="Auto" Height="Auto" Margin="10,10,10,10">
            <TextBlock HorizontalAlignment="Left" TextWrapping="Wrap" Text="Leaderboard" FontSize="30" Margin="0,0,0,30"/>

            <ListView Height="Auto" Width="500" HorizontalAlignment="Center" VerticalAlignment="Center"
                  ItemsSource="{Binding Leaderboard}" IsItemClickEnabled="False"
                  SelectionMode="None"
                  ItemTemplate="{StaticResource LeaderboardTeamTemplate}">
            </ListView>

            <TextBlock HorizontalAlignment="Left" TextWrapping="Wrap" Text="Team name" FontSize="30" Margin="0,30,0,10"/>
            <Button Content="Next clue" HorizontalAlignment="Stretch" Command="{Binding JoinTeam}"/>
        </StackPanel>
    </Grid>
</Page>
