<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Bootcamp2015.AmazingRace.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:maps="using:Windows.UI.Xaml.Controls.Maps"
    x:Class="Bootcamp2015.AmazingRace.Views.MapPage"
    xmlns:viewModels="using:Bootcamp2015.AmazingRace.ViewModels"
    xmlns:micro="using:Caliburn.Micro"
    micro:Bind.AtDesignTime="True"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <maps:MapControl
            HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
            MapServiceToken="{Binding MapToken}"
            Center="{Binding CurrentPosition}"
            ZoomLevel="10">
        </maps:MapControl>

        <StackPanel Grid.Row="1" VerticalAlignment="Bottom" Orientation="Horizontal" Background="#77777777">
            <!--<Button Content="Locate" Click="OnRefreshClick" Margin="5,0" HorizontalAlignment="Center" />-->
        </StackPanel>
    </Grid>
</Page>
